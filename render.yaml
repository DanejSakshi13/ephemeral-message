services:
  - type: web
    name: ephemeral-message
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ephemeral_message_api:app --host 0.0.0.0 --port 10000
    plan: free
